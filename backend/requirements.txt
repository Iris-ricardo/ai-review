# ── Web Framework ────────────────────────────
fastapi==0.138.0
uvicorn==0.40.0
python-multipart==0.0.32

# ── Database ──────────────────────────────────
sqlalchemy==2.0.43

# ── Document Parsing ──────────────────────────
PyMuPDF==1.28.0
python-docx==1.2.0
lxml==5.3.0

# ── LLM ───────────────────────────────────────
openai==2.45.0
httpx==0.28.1

# ── Report Generation ─────────────────────────
# M4 审查报告 PDF 使用 reportlab（纯 Python，无外部依赖，中文字体嵌入）
# weasyprint 已移除（依赖 GTK 运行库，Windows 不稳定）
reportlab==5.0.0
openpyxl==3.1.5

# ── Configuration ─────────────────────────────
pydantic==2.12.4
pydantic-settings==2.12.0
python-dotenv==1.1.0
pyyaml==6.0.3
