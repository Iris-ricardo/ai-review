[pytest]
; basetemp 落在 backend/.pytest-basetemp（CWD 相对、每次会话结束由 conftest 清理），
; 避免打包机器遗留的受限 ACL 目录（如 %TEMP%\pytest-of-<user>）破坏 tmp_path fixture。
; 本项目未使用 pytest 缓存（--lf 等），禁用 cacheprovider 以免向仓库内 .pytest_cache 写入。
addopts = --basetemp=.pytest-basetemp -p no:cacheprovider
testpaths = tests
