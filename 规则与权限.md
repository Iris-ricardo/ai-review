# 规则与权限参考

本文件由原 `docs/检查器覆盖矩阵.md` 与 `docs/本地用户分级方案.md` 合并而成（内容照录，仅统一标题层级）。

- 第一部分：当前检查器覆盖边界与后续新增路线。
- 第二部分：本地用户角色、权限边界、审计与实施步骤。

> 规则集的**修改流程**（界面四眼审批路径、直接改 `rules/*.yaml` 的优先级陷阱、校验接口、改完后的验证闭环）见同目录 [README](README.md) 的「修改规则集：两种方式」一节。

## 目录

1. [检查器覆盖矩阵与新增路线](#1-检查器覆盖矩阵与新增路线)
2. [本地用户分级方案](#2-本地用户分级方案)

---

## 1. 检查器覆盖矩阵与新增路线

*来源：`docs/检查器覆盖矩阵.md`（原文日期 2026-07-20）*


更新日期：2026-07-20

### 1. 审计结论

当前三个内置规则集均有 20 条规则，其中 17 条为本地确定性规则，3 条为可选的 AI 语义规则。三套规则共用检查器算法，项目类型差异主要由 YAML 参数表达。

已完成两批覆盖扩展：新增 `required_fields`、`field_format`、`heading_numbering` 和 `layout_check`，并增强 `cross_field_consistency`，全部接入校级课题、大创、国自然三套规则集。业务人员可通过 YAML 调整字段、别名、归一化方式、标题编号体系、页面尺寸、边距、正文和标题样式；使用现有参数时不需要修改 Python 代码。

覆盖等级说明：

- A：可确定性自动判断，适合作为硬性规则；
- B：可以自动发现大部分问题，但需结合原文复核；
- C：仅能提供线索或要求人工确认；
- D：当前未实现。

### 2. 当前检查器矩阵

| 检查器 | 覆盖等级 | 当前能力 | 主要边界 | 新增同类规则是否改代码 |
|---|---:|---|---|---|
| `required_sections` | B | 必填章节、可选章节顺序 | 依赖标题识别，复杂表格标题和多级标题可能漏识别 | 通常只改 YAML |
| `page_limit` | A | 总页数上下限 | 封面、目录、附件是否计入口径需按指南配置扩展 | 通常只改 YAML |
| `word_limit` | B | 文本总字数限制 | 中英文、表格、公式及附件的计数口径需细化 | 通常只改 YAML |
| `font_check` | B | 正文主字体和字号 | 标题、行距、段间距、首行缩进尚未逐段覆盖 | 现有字体规则只改 YAML；新版式要改代码 |
| `figure_table_numbering` | B | 图、表编号连续性 | 章节式编号、公式编号、交叉引用尚不完整 | 复杂编号需改代码 |
| `budget_check` | B | 合计、总计和科目占比 | 多表、多级小计、单位换算、合并单元格较弱 | 简单比例只改 YAML；复杂预算需改代码 |
| `date_check` | B | 起止日期与期望月数 | 阶段计划重叠、断档、边界日期逻辑不足 | 月数只改 YAML；计划逻辑需改代码 |
| `page_number_check` | B | 页码存在性和连续性 | 罗马数字、中文页码、复杂页脚可能漏检 | 新样式多需改代码 |
| `attachment_checklist` | C | 从正文文字识别附件线索 | 不能证明独立附件文件真实存在 | 名称只改 YAML；材料包核验需改架构 |
| `signature_page` | C | 识别签字、盖章文字及图片线索 | 不能验证真实签章、签署人和有效性 | 关键词只改 YAML；验真需外部系统 |
| `cross_field_consistency` | B | 同一字段多处一致性及两个关联字段显式比较；支持文本、大小写、电话、日期、数字、列表归一化、金额单位换算和数值容差 | 无标签自然语言、复杂团队表、计算型业务逻辑仍需专用算法 | 现有归一化与字段配对只改 YAML；新计算逻辑需改代码 |
| `reference_format` | B | 本地参考文献格式线索检查 | 未完整实现 GB/T 7714，也不验证文献真实性 | 基础模式可配置；完整标准需改代码 |
| `required_fields` | B | 段落“标签：值”和表格相邻单元格的必填检查；支持别名和可选字段 | 无标签自然语言、复选框、图片字段、复杂跨单元格可能漏检 | 只改 YAML |
| `field_format` | B | 邮箱、手机号、电话、日期、身份证、数字、项目编号及自定义正则 | 只检查已识别且已填写的字段；字段必填应配合 `required_fields` | 内置格式/正则只改 YAML；新算法格式需改代码 |
| `heading_numbering` | B | 中文数字、括号数字、阿拉伯数字、多级小数编号；检查层级跳级、编号断档、父级不匹配和同级样式混用 | 依赖标题识别；无编号视觉标题和非常规编号需复核 | 编号要求和允许样式只改 YAML；新编号语法需改代码 |
| `layout_check` | B | A4/A3/Letter、横纵向、最小边距、正文主字号/对齐/行距/首行缩进、分级标题字体字号粗体和对齐 | PDF 行距与缩进来自坐标测量；复杂分栏、文本框、页眉页脚可能误判 | 现有页面和样式参数只改 YAML；新布局维度需扩展 IR |
| `anonymity_check` | B | AI 辅助匿名信息检查 | 依赖模型、文本抽取和发送范围 | 提示参数可配置；可靠本地扫描需改代码 |
| `title_content_match` | B | AI 辅助题目与内容匹配 | 属于语义判断，不适合作为唯一硬性结论 | 通常需调整提示词或模型策略 |
| `cross_consistency_semantic` | B | AI 辅助跨章节语义一致性 | 延迟、成本和模型稳定性限制 | 通常需调整提示词或模型策略 |

`required_fields` 与 `field_format` 应组合使用：前者判断“有没有填”，后者判断“已填写的值是否合法”。格式检查不会把缺失字段重复报错。

### 3. 字段规则配置方式

必填字段示例：

```yaml
- id: C017
  type: required_fields
  severity: error
  params:
    fields:
      - name: 项目负责人
        aliases: [负责人, 申请人]
      - name: 推荐意见
        required: false
```

格式字段示例：

```yaml
- id: C018
  type: field_format
  severity: warning
  params:
    fields:
      - {name: 电子邮箱, aliases: [邮箱, Email], format: email}
      - {name: 联系电话, aliases: [手机], format: cn_mobile}
      - {name: 项目编号, format: project_code}
      - {name: 自定义编号, pattern: "^AI-[0-9]{6}$", expected: "AI-加6位数字"}
```

支持的内置格式为：`email`、`cn_mobile`、`phone`、`date`、`cn_id`、`number`、`project_code`。指南自动生成规则也只允许这些格式；未知格式会进入丢弃清单，等待人工处理。

跨字段一致性示例：

```yaml
- id: C012
  type: cross_field_consistency
  severity: warning
  params:
    fields:
      - {name: 项目负责人, aliases: [负责人, 申请人], normalize: text}
      - {name: 联系电话, aliases: [手机], normalize: phone}
    comparisons:
      - left: {name: 预算总额, aliases: [经费总额]}
        right: {name: 经费合计, aliases: [预算合计]}
        normalize: number
        tolerance: 0.01
```

标题和版式示例：

```yaml
- id: C019
  type: heading_numbering
  severity: warning
  params:
    skip_pages: [1]
    require_numbering: false
    allow_mixed_styles: false
    allowed_styles:
      "1": [chinese, chapter, arabic]
      "2": [chinese_parenthesized, arabic_parenthesized, decimal]

- id: C020
  type: layout_check
  severity: warning
  params:
    page: {size: A4, orientation: portrait, tolerance_pt: 14}
    margins_mm: {left_min: 15, right_min: 15, top_min: 15, bottom_min: 15}
    body: {min_size: 9, max_size: 14, line_spacing_min_pt: 11}
    headings:
      "1": {min_size: 12, max_size: 22, bold: true}
```

### 4. 后续新增路线

| 批次 | 优先级 | 工作项 | 目标验收 |
|---|---:|---|---|
| 已完成 | P0 | 增强 `cross_field_consistency` | 多字段、归一化、金额换算、容差、跨字段比较与证据定位已实现 |
| 已完成 | P0 | 新增 `heading_numbering` | 层级跳级、编号不连续、父级错误和样式混用已实现 |
| 已完成 | P1 | 新增 `layout_check` | 页面、方向、边距、正文行距/缩进和分级标题基础样式已实现 |
| 下一批 | P1 | 深化复杂版式 | 增加分栏、段前段后、页眉页脚、文本框识别和真实模板校准 |
| 下一批 | P1 | 增强预算、日期、图表、参考文献 | 使用复杂表格和章节式编号样例验证；明确不可自动证明项 |
| 第四批 | P1 | 新增 `application_package` | 支持多文件材料包，核验文件名、类型、大小、附件存在性和跨文件字段一致性 |
| 第四批 | P2 | 项目类型专用规则 | 大创成员/导师限制、国自然申请代码与限项、项目类别专用预算规则 |
| 外部集成 | P2 | 权威数据核验 | 对接人事、历史项目、电子签章、文献和查重系统后再做真实性判断 |

### 5. 每批开发与验收口径

每新增或增强一个检查器，都应同时交付：

1. 参数结构和错误配置处理；
2. 正常、缺失、边界、误报与解析异常测试；
3. 至少一份命中样例和一份不命中样例；
4. 三套规则集的适用性判断，而不是机械复制规则；
5. 覆盖矩阵、规则依据和人工复核边界更新；
6. 全量后端测试、前端构建与一次真实上传审查验收。

最终覆盖率不能只按“检查器数量”计算。应针对每份真实指南逐条建立：指南条款、检查器、自动化等级、测试样例、漏报/误报记录和人工审核责任。只有真实脱敏模板回归通过，才能认为对应业务条款已覆盖。

---

## 2. 本地用户分级方案

*来源：`docs/本地用户分级方案.md`（原文日期 2026-07-21）*


### 1. 目标

把当前共享 `ADMIN_TOKEN` 的本地单管理员模式，升级为适合竞赛演示和单机部署的用户分级体系。首版不追求复杂组织架构，重点解决“谁能上传、谁能改规则、谁能发布、谁能停用或删除”的权限边界。

### 2. 角色划分

| 角色 | 定位 | 主要权限 |
| --- | --- | --- |
| 普通用户 | 申报书提交者 | 上传文档、选择已发布规则集、查看自己的审查结果 |
| 规则维护员 | 规则配置人员 | 新建规则集草稿、编辑规则、开启/关闭规则、运行样例测试 |
| 规则审核员 | 发布审核人员 | 审核草稿、驳回草稿、发布规则版本 |
| 管理员 | 平台运维人员 | 停用/恢复规则集、归档规则集、管理普通用户和维护员 |
| 超级管理员 | 系统负责人 | 初始化系统、管理管理员、危险删除、模型密钥和系统配置 |

### 3. 权限边界

| 功能 | 普通用户 | 规则维护员 | 规则审核员 | 管理员 | 超级管理员 |
| --- | --- | --- | --- | --- | --- |
| 上传申报书 | ✓ | ✓ | ✓ | ✓ | ✓ |
| 查看本人审查结果 | ✓ | ✓ | ✓ | ✓ | ✓ |
| 查看全部审查任务 |  |  | ✓ | ✓ | ✓ |
| 新建规则集草稿 |  | ✓ |  | ✓ | ✓ |
| 修改规则内容 |  | ✓ |  | ✓ | ✓ |
| 运行发布前样例测试 |  | ✓ | ✓ | ✓ | ✓ |
| 提交规则审核 |  | ✓ |  | ✓ | ✓ |
| 驳回规则草稿 |  |  | ✓ | ✓ | ✓ |
| 发布规则版本 |  |  | ✓ | ✓ | ✓ |
| 停用/恢复规则集 |  |  |  | ✓ | ✓ |
| 删除未发布规则集 |  | ✓ |  | ✓ | ✓ |
| 删除已发布但未引用规则集 |  |  |  |  | ✓ |
| 管理用户和角色 |  |  |  | ✓ | ✓ |
| 管理模型密钥与系统配置 |  |  |  |  | ✓ |

### 4. 删除与停用策略

- 未发布规则集：允许规则维护员、管理员或超级管理员删除。
- 已发布且被历史任务引用：不允许物理删除，只能停用或归档。
- 已发布但未被任何任务引用：首版建议仍只允许超级管理员删除，并要求二次确认。
- 内置规则集：默认禁止删除，只允许停用、复制、恢复。
- 前端按钮禁用时必须展示原因，避免只出现禁止光标。

### 5. 后端改造步骤

1. 新增用户表。
   - `users`
   - 字段：`id`、`username`、`password_hash`、`display_name`、`status`、`role`、`created_at`、`last_login_at`

2. 新增登录认证。
   - `POST /api/v1/auth/login`
   - `POST /api/v1/auth/logout`
   - `GET /api/v1/auth/me`
   - 本地版可使用账号密码和服务端签名 token。

3. 新增权限依赖。
   - `require_capability("ruleset.edit")`
   - `require_capability("ruleset.publish")`
   - `require_capability("ruleset.disable")`
   - `require_capability("users.manage")`

4. 替换 `ADMIN_TOKEN`。
   - 过渡期保留 `ADMIN_TOKEN`，映射为虚拟用户 `bootstrap-admin`。
   - 创建首个超级管理员后，可在配置中关闭兼容模式。

5. 改造审计记录。
   - 记录真实 `actor_user_id`、`actor_name`、`role`。
   - 规则保存、提交、发布、驳回、停用、删除、用户角色变更都要入审计。

### 6. 前端改造步骤

1. 增加登录页。
2. 增加当前用户状态。
3. 根据 `GET /auth/me` 返回的 `capabilities[]` 控制菜单和按钮。
4. 禁用按钮时显示明确原因。
5. 增加用户管理页。
6. 增加规则操作审计中的真实操作人展示。

### 7. 首版验收标准

- 五类角色能分别登录。
- 无权限接口返回 `403`，不能只靠前端隐藏按钮。
- 普通用户看不到规则管理入口。
- 规则维护员不能发布规则。
- 规则审核员不能直接编辑草稿内容。
- 管理员可以停用规则集，但不能管理模型密钥。
- 超级管理员可以完成初始化和危险操作。
- 所有规则管理写操作都有真实操作人审计。
- 发布流程支持“提交人与发布人分离”的四眼原则。

### 8. 建议实施顺序

1. 账号登录与 `auth/me`。
2. 用户表和角色字段。
3. 后端权限依赖。
4. 前端菜单和按钮权限控制。
5. 规则管理接口逐项接入权限。
6. 用户管理页。
7. 审计记录接入真实用户。
8. 四眼审批限制。

### 9. 当前项目状态

当前项目已实现本地竞赛演示版用户分级首版：

- 已有本地账号密码登录；
- 已有五类默认演示账号；
- 已有 `GET /api/v1/auth/me`、登录、退出和用户列表接口；
- 规则管理接口已按权限点限制；
- 前端导航会按角色显示“规则集管理”和“用户管理”；
- 规则管理写操作审计已记录真实登录用户名。

仍需后续增强：

- 在线创建用户、重置密码、停用用户和修改角色；
- 严格四眼审批已实现：提交人不能审核或发布自己的版本；
- 普通审查任务的数据归属隔离；
- 登录会话撤销和密码首次登录强制修改。

### 10. 2026-07-21 完整化改造结果

本轮已把“存在角色字段”升级为实际可使用的多入口权限体系：

- 登录后统一进入身份工作台，工作台说明当前角色职责和权限边界；
- 单份审查、批量审查、规则集中心、用户管理四类入口按能力点显示；
- 路由守卫禁止通过手工输入地址进入无权页面；
- 后端接口独立校验能力点，前端隐藏菜单不作为安全边界；
- 文档、单份审查任务和批次写入 `owner_id`，普通用户只能读取本人数据；
- 规则审核员、管理员和超级管理员拥有全部任务查看范围；
- 用户管理已支持新增账号、修改显示名称与角色、启用/停用账号、重置密码；
- 管理员不能创建、授予或修改超级管理员，账号不能停用自身；
- `Authorization: Bearer` 专用于登录会话，`X-Access-Token` 专用于外围访问控制，二者不再混用；
- SQLite 旧库采用增量字段迁移，不要求删除原有审查数据。

仍属于后续安全增强项：服务端会话撤销、首次登录强制改密和更完整的用户管理操作审计表。

### 11. 2026-07-21 严格规则审批流程

规则治理已经从“建议分工”升级为后端强制状态机：

1. 规则维护员创建、编辑和保存草稿；
2. 当前内容必须通过样例测试，否则不能提交；
3. 提交时必须从有效审核员中指定一人，提交人不能指定自己；
4. 提交后内容锁定，只有被指定审核员可以审核；
5. 创建人或提交人不能审核、驳回或发布自己的版本；
6. 审核通过后进入 `approved` 待发布状态，不能从 `submitted` 直接发布；
7. 驳回必须填写整改原因，版本进入 `rejected`，维护员修改保存后回到草稿；
8. 只有完成审核的指定审核员可以发布；
9. 发布时再次校验当前内容哈希对应的样例测试，历史测试不能解锁新内容；
10. 创建人、提交人、指定审核员、审核人、驳回原因、发布人和时间均写入版本记录与审计日志。

管理员已经移除规则编辑、提交、审核和发布权限，只负责用户、批量任务、规则集停用/恢复等平台运行管理。超级管理员具备多种能力，但仍受“不能审核或发布本人创建/提交版本”和“必须是指定审核员”的流程约束。
